# Trinity V10.31AC — AI 제어 규칙

## 필수 작업 규칙
- 함수 삭제/이동 전 `grep -rn "함수명" --include="*.py"` 실행하여 외부 참조 확인
- `apply_order_results` 등 시그니처 변경 시 모든 호출부 동시 수정 (grep으로 확인)
- 수정 완료 후 `python3 -c "import ast; ast.parse(open('파일').read())"` 문법 검증
- BC/CB는 **모든** 슬롯 카운팅에서 제외 (slot_manager, planners._core_long/short, _HEDGE_ROLES_SLOT)
- DCA 처리 시 trim_trail_active / trim_trail_max 리셋 + stale tp1 필드 정리
- trail close는 p["amt"] 전량 — 잔량 남으면 바이낸스 유령 포지션 발생
- pending 구조는 fill 이벤트 없으면 영원히 발사 안 됨 → 즉시 발사 필요 시 intents.append
- **★ V10.30: .md 파일 읽고 수정 후 반드시 업데이트**
- **★ V10.30: DCA는 단일 경로(_place_dca_preorders LIMIT만). plan_dca 함수 자체 제거됨 (V10.31c)**
- **★ V10.30: DCA 주문 전 calc_tier_notional - 현재보유 검증 필수 (과주문 방지)**
- **★ V10.30: FC/TRAIL_ON 시 거래소 잔존 주문 즉시 취소 (_FC_EXCHANGE_CANCEL)**
- **★ V10.31b: 미장전 포지션 정리. ET08:00 진입차단 → ET08:30 전포지션 시장가정리. DST자동, 주말/공휴일 스킵** (★ V10.31j: 비활성 / 주석처리 — 함수 정의 유지)
- **★ V10.31c: ROI 계산은 `v9.utils.utils_math.calc_roi_pct()` 단일 경로 (인라인 계산 금지)**
- **★ V10.31c: SYM_MIN_QTY는 부팅 시 ccxt load_markets에서 동적 채움 (`_load_sym_limits_from_ccxt`). 하드코딩은 fallback**
- **★ V10.31c: precision/notional 에러(`-1111`, `-4003`, `-4005`, "minimum amount precision", "minimum notional")도 exit_fail_cooldown 트리거**
- **★ V10.31j: TRIM 임계는 `calc_dynamic_trim_thresh(tier, worst_roi)` 단일 경로. T2 worst≤-2→0.5, T3 worst≤-5→-0.5 디펜스 모드. `calc_trim_price()` 호출 시 worst_roi 전달 필수**
- **★ V10.31j: T3 시간 컷 분기 — TREND는 `plan_t3_3h_cut_trend`(3h~4h), MR은 `plan_t3_8h_cut`(7h~8h). 각자 `_t3_3h_step`/`_t3_8h_step`, `is_t3_3h_limit`/`is_t3_8h_limit` 필드 완전 분리 (중복 금지)**
- **★ V10.31j: DCA 체결 시 `_t2_def_logged`/`_t3_def_m5_logged` 플래그 리셋 필수 (tier별 독립 디펜스 로그)**
- **★ V10.31k: MR 진입 루프 내 `_tick_new_long`/`_tick_new_short` 카운터 유지 필수 — `st` 미갱신 상태에서 슬롯 초과 방지**
- **★ V10.31k: Portfolio TP — peak ≥ 1% + drop ≥ f(peak) (J안, tiered drop). 세션 경계는 ★ KST 09:00 (UTC 00:00) — 텔레그램 일일 수익률 리셋 시각과 통일. step 3 완료 시 자연 쿨다운 (_session_start 갱신). K gate 비활성 (PTP_AVG_TIER_GATE=0.0)**
- **★ V10.31k: 일일 시간 경계는 시스템 전체 KST 09:00 (= UTC 00:00) 통일. _daily_pnl_report, PTP, 향후 일일 기반 모든 로직이 동일 기준 사용**
- **★ V10.31l: PTP 재시작 시 `_load_today_balance_stats()`로 balance.csv에서 오늘 UTC 00:00 이후 start/peak 복원. 서버 타임존 UTC 가정 (balance.csv 시각=UTC). 복원 실패 시 current_balance 폴백**

## 모듈별 상세 문서 (관련 수정 시 반드시 참조)
- 슬롯/리스크 수정 → `docs/SLOTS.md`
- 신규 진입/MR 수정 → `docs/OPEN.md`
- DCA 수정 → `docs/DCA.md`
- Trim/TP1 수정 → `docs/TRIM.md`
- Trail/청산 수정 → `docs/TRAIL.md`
- BC(Beta Cycle) 수정 → `docs/BC.md`
- CB(Crash Bounce) 수정 → `docs/CB.md`
- TREND 수정 → `docs/TREND.md`
- 유니버스/데이터 수정 → `docs/UNIVERSE.md`
- 수익성 지표/로깅 인프라 → `docs/OBSERVABILITY.md` (V10.31d 신규)
- 전체 아키텍처 → `ARCHITECTURE_V10.26.md`

## 파일 수정 금지 목록
- `execution_engine.py` — 주문 실행 코어 (잘못되면 실주문 사고)
- `position_book.py` — 포지션 상태 (잘못되면 유령 포지션)
- `telegram_deploy_bot.py` — 배포 인프라

## 버그 히스토리 (재발 방지)
| 날짜 | 버그 | 근본 원인 | 체크 대상 |
|------|------|-----------|-----------|
| 04-12 | LINK trim 영구 차단 | DCA 시 tp1_limit_oid 미클리어 | strategy_core.py DCA 블록 |
| 04-12 | TREND_NOSLOT 미발사 | pending 구조 + MR fill 없음 | planners.py trigger_side=None 블록 |
| 04-12 | BC가 MR 슬롯 점유 | _core_short에 BC 미제외 | planners.py line 613 |
| 04-12 | _wider_regime 크래시 | 함수 삭제 후 외부 import 미확인 | strategy_core.py DCA import |
| 04-12 | TREND 3개 동시 진입 | NOSLOT 쿨다운 누락 | _open_dir_cd 적용 |
| 04-13 | T3 market DCA trim 미배치 | strategy_core에 trim_to_place 미세팅 (HIGH 레짐 market DCA만 발생) | strategy_core.py DCA 블록 |
| 04-13 | 죽은 코드 정리 | TP2_PCT/TP2_PARTIAL_RATIO/TRIM_PREORDER_ROI 미사용 | config.py, planners.py import |
| 04-13 | trail max_roi 2.0 미만 발동 | TP1 체결 시 max_roi_seen을 snapshot ROI로 덮어씀 | strategy_core.py TP1 블록 |
| 04-13 | 스큐 로직 전면 제거 | TREND 진입이 스큐 해소 담당. heavy TP인하/DCA가속/T3방어/SKEW_E30 삭제 | planners/config/runner |
| 04-13 | 429 Rate Limit | balance 3초/ticker 2초 캐시 + OHLCV 15초 + sem6 + 429 백오프 | market_snapshot.py |
| 04-13 | BC/CB 동기 fetch 블로킹 | bc_on_tick/cb_on_tick을 asyncio.to_thread로 감싸기 | runner.py |
| 04-13 | DCA 선주문 도입 | T1 진입 시 T2 limit 선배치, DCA fill 시 다음 tier 배치 (maker 수수료) | runner/planners/strategy_core |
| 04-14 | side=None crash | trigger_side=None 시 continue 누락 → ccxt crash | planners.py NOSLOT 블록 |
| 04-14 | DCA 후 ghost trailing | runner DCA fill 시 step/tp1_done 미리셋 → 트레일+TRIM 차단 | runner.py DCA fill handler |
| 04-14 | RESIDUAL 무한루프 | float epsilon(2.84e-14) + reduce_fail_cooldown 무시 | hedge_engine.py |
| 04-14 | FC 후 DCA 좀비 | FC 시 거래소 LIMIT 미취소 → 빈 슬롯에 DCA 체결 | strategy_core.py + runner.py |
| 04-14 | BC 즉사 | 1h excess 뜨거운데 1d 정규화만 보고 진입 → REHEAT 즉사 | beta_cycle.py baseline 도입 |
| 04-14 | T2 정규 TP1 빠짐 | T2+에서 trim 미발동 시 정규 TP1 fallback → 전량 사망 | planners.py continue 추가 |
| 04-14 | DCA 과주문 (AAVE 3배) | 목표 노셔널 검증 없이 개별 weight 기반 주문 | planners.py + runner.py 가드 |
| 04-14 | DCA 이중 경로 | plan_dca(시장가) + DCA_PRE(LIMIT) 동시 발동 | plan_dca 제거, 단일 경로 |
| 04-15 | T2+ trim trail 미발동 | plan_tp1 guard(tp1_preorder_id/limit_oid/step 등)가 T2+ trail까지 차단 | V10.31b: 전 tier trail 통합, preorder 시스템 제거 |
| 04-15 | TIA T2 포지션 비대화(3728) | GHOST_CLEANUP이 거래소 DCA limit 미취소 → 다음 OPEN에 옛날 DCA 체결 | V10.31b: GHOST_CLEANUP 시 PENDING_LIMITS+dca_preorders 취소큐 추가 |
| 04-15 | trim이 limit으로 나감 | plan_trim_trail에 force_market 미설정 → limit placed → sync 불일치 → ghost | V10.31b: plan_trim_trail metadata에 force_market:True 추가 |
| 04-15 | 재시작 후 stale DCA 잔존 | _PENDING_LIMITS 유실 + dca_preorders OID만 남음 → stale pop만 하고 거래소 미취소 | V10.31b: stale 감지 시 ex.cancel_order 호출 + GHOST에 FC_EXCHANGE_CANCEL 추가 |
| 04-15 | calc_roi_pct UnboundLocalError | TRAIL_ON 블록 내 로컬 import가 TP1 블록까지 오염 → TP1 체결 시 크래시 → 롱 소실(L2→L0) | V10.31b: apply_order_results 함수 최상단으로 import 이동 |
| 04-13 | RESIDUAL_CLEANUP 무한루프 | hedge_engine이 reduce_fail_cooldown 체크, runner는 exit_fail_cooldown 세팅 → 필드명 불일치 → 쿨다운 무시 | V10.31b: exit_fail_cooldown_until로 통일 |
| 04-15 | SEI CorrGuard -5.3% 조기컷 | corr<0.5+ROI<-4% → 강제청산이 T3 회복 차단 | V10.31b: CorrGuard 제거 (pure trim/trail 신뢰) |
| 04-15 | Zombie 강제청산 | 슬롯풀 시 T1/T2 조건부 청산 → 회복 차단 | V10.31b: Zombie 로직 제거 |
| 04-15 | TREND_COMP 쿨다운 막힘 | DOT TRAIL_ON 15분 후 SEI MR이 DOT를 companion 선택 → DOT 쿨다운에 걸려 REJECT | V10.31b: entry_type=TREND는 쿨다운 면제 |
| 04-17 | trim 선주문 가격 stale | DCA로 EP 변경 후 기존 trim limit 가격 미갱신 → 구 EP 기준 +3.3%에서 대기 (정상 1.5%) | V10.31b: _place_trim_preorders에서 매 틱 가격 검증, 0.1% 이상 차이 시 취소+재배치 |
| 04-17 | TREND score 1.0~2.0 구간 손실 | 애매한 트렌드 세기 → DCA까지 끌려감 → T3 FC | V10.31b: score 1.0~2.0 TREND 진입 차단 (COMP+NOSLOT 모두) |
| 04-17 | BC 활성 시 MR 레버리지 초과 | BC가 잔고 사용 중인데 MR이 전체 잔고 기준 사이징 → 실질 레버리지 초과 | V10.31b: _mr_available_balance() — BC 노셔널 차감 후 MR 사이징 (진입/DCA/trim 전부) |
| 04-17 | PnL 과장 (내부계산 vs 바이낸스) | OrderResult에 realized_pnl 미포함 → 내부 (exit-ep)×qty 계산이 trim 과다매도 등으로 부풀림 | V10.31b: OrderResult.realized_pnl 추가 + order_router/runner에서 바이낸스 trades realizedPnl 추출 |
| 04-17 | trim PnL 3배 뻥튀기 | `filled_qty × price_diff × LEVERAGE` — qty가 이미 레버리지 반영 수량인데 LEVERAGE(3) 추가 곱셈 | V10.31b: `× LEVERAGE` 제거 + realizedPnl 우선 사용 |
| 04-18 | RESIDUAL_CLEANUP 무한재시도 (재발) | `_record_fail_cooldown`이 `-2022/ReduceOnly`만 체크 → Binance precision 에러(`-1111` 등)는 쿨다운 미세팅 → 3초마다 무한재시도. 추가로 SYM_MIN_QTY 하드코딩에 대다수 심볼 누락 → `_res_min_qty=1.0 fallback` → `amt < min*2` 오판으로 의미있는 수량을 dust로 청산 | V10.31c: (a) order_router + runner 이중 방어 — precision/notional 에러군 쿨다운 60초 (b) `_load_sym_limits_from_ccxt` — 부팅 시 ccxt load_markets에서 동적 채움 |
| 04-18 | TREND_SCORE_SKIP 3초 스팸 | `setattr(plan_open, ...)` 방식 쿨다운이 실제 작동 안 함 → 동일 sym+sig 조합 매 틱 로깅 | V10.31c: 모듈 레벨 `_TREND_SKIP_LOG_CD: Dict[str, float]`로 교체 |
| 04-18 | log_skew.csv 죽은 로깅 | 스큐 로직은 V10.30에서 전면 제거되었으나 logger 호출부 잔존 → 969KB/주 누적 | V10.31c: log_skew 함수/호출/schema/config 전부 제거. _urgency_score 계산은 유지 |
| 04-18 | ROI 인라인 중복 | strategy_core._log_pos / runner 포지션 스냅샷에서 `(curr_p-ep)/ep * LEV * 100` 인라인 계산 — calc_roi_pct와 중복, 수정 시 동기화 위험 | V10.31c: 두 위치 모두 `calc_roi_pct()` 호출로 통일 |
| 04-18 | plan_dca 죽은 코드 | V10.30에서 호출 제거되었으나 함수 정의(276줄) 잔존 | V10.31c: 함수 삭제 + docstring 정정 |
| 04-18 | TREND_MIN_SCORE 미사용 config | config.py에 정의되고 import만 되어 있음. 실제 조건 체크는 `_TR_MIN=0.5` 하드코딩 사용 | V10.31c: config 제거 + import 제거 + _calc_trend_score docstring 정정 |
| 04-18 | BTC 방향성 필터 효과 불명 | 로그만 보고 "역방향 MR이 T3 FC 원인"이라 가정했으나 결과론적. 실측 필요 | V10.31c: TREND_FILTER_SIM shadow logging 신설. Strict(1h≤-1.5%/6h≤-4%/dev≤-3%) + Loose(1h≤-0.7%/6h≤-2%/dev≤-1.5%) 두 임계값 병렬 기록. MR 청산 시점에 "필터가 차단했다면 놓쳤을 ROI" 집계. 실전 진입은 차단하지 않음 (shadow only) |
| 04-18 | BC trail 조기 청산 의심 | AAVE 3.5%/FIL 3.6% 마감 — activation 3%/floor 1.5% 설계 예민함 체감. 근데 peak_roi 미로깅이라 실제 giveback 측정 불가 | V10.31c: (a) TRAIL 평가에서 `h_1h` wick 제거 — trail은 "추세 꺾임 확인"이므로 wick 스파이크로 발동하면 노이즈 과민반응. SL은 wick 유지(손실 방어). (b) `bc_peak_roi` 추적 + `BC_EXIT` 로그에 exit/peak/giveback 기록 — 2~3주 데이터 수집 후 activation/floor 재검토 |
| 04-18 | 슬롯 5/5 → 4/4 | MAX_LONG/SHORT=5로 되어 있으나 실운영은 MAX_MR_PER_SIDE=4에 막혀 4/4였음. 설정과 실제 불일치 | V10.31c: MAX_LONG/SHORT 4/4, TOTAL_MAX_SLOTS 10→8. 논리 변화 없음, 명시화만 |
| 04-18 | 선주문 취소 로직 위치 혼란 | `_cancel_tp1_preorder` / `_cancel_trim_preorders`가 runner.py에 있고, 거래소 주문 취소 API 호출인데 실행 책임이 runner에 분산됨 | V10.31c: 구현체를 `v9/execution/order_router.py`로 이동 (`cancel_tp1_preorder`, `cancel_trim_preorders`). runner.py는 wrapper 유지하여 외부 호출 호환. ~60줄 runner.py에서 제거 |
| 04-18 | 대시보드 경로 불일치 | status_writer.py가 `v9/v9_status.json` 쓰고 status_server.py가 `프로젝트루트/v9_status.json` 읽음. 서로 다른 파일 → 대시보드 한 번도 작동 안 함 | V10.31c: status_writer.py `_BASE_DIR`에 `..` 추가하여 프로젝트 루트 가리키게 수정 |
| 04-18 | status_server 자동 기동 부재 | main.py가 runner만 호출, status_server는 수동 실행 필요 → 기동 안 됨 | V10.31c: main.py에 `_status_server_loop` 백그라운드 thread 추가. 크래시 시 10초 후 재기동 (무한 재시작) |
| 04-18 | `NameError: LEVERAGE not defined` | 함수 내부에서 `from v9.config import LEVERAGE`를 if/elif 분기 안에서 조건부 import. 특정 경로로 실행 시 LEVERAGE 미정의 → 워치독 에러 | V10.31c: 4개 파일(strategy_core, runner, status_writer, risk_manager, dca_engine) module-level에 LEVERAGE/calc_roi_pct 단일 import 승격. 함수 내 비-alias 중복 import 전수 제거 (local shadow 방지). AST 검증으로 잔존 0건 확인 |
| 04-20 | Phase 1 dead code 정리 | `TRIM_TRAIL_FLOOR`가 V10.31c에서 로직은 제거됐으나 config 정의 + planners 2곳 import + docs 체크리스트 잔존 (dead symbol) / trail gap 주석 "fixed 0.5" vs 실코드 `0.3` 불일치 4곳 (planners 3, hedge_engine 1) | V10.31d Phase 1: (a) `TRIM_TRAIL_FLOOR` 정의·import·doc 체크리스트 전수 제거 (import chain 검증으로 잔존 0 확인) (b) 주석 "0.5"→"0.3" 4곳 수정 |
| 04-20 | 수익성 검증 불가 구조 | trades.csv PnL은 gross (수수료 차감 전). `runner.py:1103`에서 `_rcomm` 추출까진 하나 `info["_commission"]`으로 세팅 안 하고 버림 → log_trade 호출부에 fee 도달 불가. 펀딩비는 로깅 자체 부재. 결과: 약손실 원인(수수료·펀딩 누수) 시스템적 측정 차단 | V10.31d: (a) `OrderResult.fee_usdt` 필드 추가 (b) order_router의 trades 루프에서 `fee.cost`/`info.commission` 추출 (c) runner TP1_LIMIT_FULL + strategy_core TRAIL/CLOSE/FC 경로에서 log_trade에 fee 전달 (d) TRADES_COLUMNS에 `fee_usdt` **맨 뒤** 추가 (기존 파싱 인덱스 호환성 유지) |
| 04-20 | 펀딩비 완전 블랙홀 | 8시간마다 정산되는 펀딩비가 잔고에만 반영되고 분리 로깅 없음 → 특정 심볼/시점 누수 추적 불가 | V10.31d: (a) FUNDING_COLUMNS 신설 (time/symbol/funding_usdt/funding_rate/position_amt) (b) `log_funding()` 헬퍼 (c) `_funding_fetch_loop` 백그라운드 태스크 — 1h 주기 `ex.fetchFundingHistory(None, since, 500)`. 중복 방지 last_ts_ms + csv 마지막 줄 복원. 첫 실행 48h / 이후 2h 창 |
| 04-20 | trades.csv 스키마 마이그레이션 | `fee_usdt` 컬럼 추가로 기존 18컬럼 → 신규 19컬럼. `_append_csv`는 기존 헤더 유지 → 데이터 19개/헤더 18개 불일치. 파싱 자체는 split index 방식이라 동작하지만 pandas/외부 분석 시 마지막 컬럼 누락 | V10.31d: `_migrate_log_trades_schema()` 부팅 시 1회 실행. 헤더에 `fee_usdt` 없으면 기존 파일을 `.pre_v10_31d.csv`로 rename → `_append_csv`가 자동으로 신규 19컬럼 헤더 생성. **배포 시 재시작 필수** |
| 04-20 | 대시보드 성과 지표 부재 | MDD/Sharpe/CAGR 없음 → "잘하고 있나"를 감으로만 판단 | V10.31d: `_compute_perf_metrics()` — log_balance.csv 일별 마감 잔고 추출 → MDD(peak 대비 낙폭), Sharpe(daily return std × √365, rf=0), 누적 수익률. 신뢰도 경고 n<7 "무의미"/n<30 "낮음"/n<90 "참고용". 대시보드 인사이트 탭에 카드 + 7d 수수료·펀딩 누수 카드 추가. status_server.py `renderInsight` 확장 |
| 04-20 | 주의 — Sharpe 조기 신호 오독 위험 | 현재 데이터 n=2~9일. Sharpe 계산은 되지만 통계적 유의성 거의 없음 (±100%+ 오차). 대시보드 표시값을 "잘하고 있다"의 근거로 쓰면 위험 | V10.31d: 설계상 조치 — `warning` 필드로 n 경고 노출, 대시보드에 "n=X일" 부제 병기. **운영 원칙**: 30일 미만 Sharpe로 전략 변경 판단 금지. 90일+ 누적 후에만 유의미 |
| 04-20 | 신규 진입 throttle 과도 | OPEN_DIR_COOLDOWN_SEC=10분이 시간당 방향당 6건 상한선을 강제. HF_MR 12h 부재 + 롱 풀 L4/4 포화 상태에서 숏 진입까지 차단되어 시간당 0~1건 진입. 로그 실측으로 17:06 ETH 숏 발사 이후 17:16까지 숏 전체 쿨다운 확인. "진입 더디다" 체감의 직접 원인 | V10.31d: OPEN_DIR_COOLDOWN_SEC=0. 체크 로직 3곳(planners.py:915/969 진입 차단, 1091/1270 타임스탬프 세팅) 유지 — 0이면 `now_ts < now_ts`로 즉시 통과. 변수/로직 자체 제거는 Phase 3 dead code 정리로 미룸. **고지된 리스크**: 04-18 연쇄 FC 같은 상황에서 시간당 방향당 진입 상한선이 없어져 heavy side 가속 가능. 하지만 쿨다운이 실제 방어 효과를 냈다는 [실측] 근거는 없었음 (내 이전 주장은 [직관]이었음을 인정) |
| 04-20 | 일회성 마이그레이션 성공 로그 제거 | `_migrate_log_trades_schema()`의 성공 print가 V10.31d 배포 후 영원히 트리거될 일 없음 (기존 파일은 이미 백업됐고 앞으로는 항상 `fee_usdt in existing_cols`로 early return). 부팅마다 dead 코드 경로 | V10.31d 마이너: 성공 print 삭제. 실패 print(에러 추적용) + 스킵 로그(진단용)는 유지. 앞으로 심을 로그는 "정기 반복"이냐 "일회성"이냐로 분류해 일회성은 한 번 검증 후 제거 원칙 |
| 04-20 | OPEN_DIR_COOLDOWN 완전 제거 | V10.31d에서 값=0으로 무력화했던 것을 변수/체크/세팅/save/restore 전부 삭제 (Phase 3 dead code 정리). 소스 참조 0건 확인 | V10.31d-3: planners.py에서 정의·체크 2곳·세팅 2곳·global·save_strategy_state·restore_strategy_state 전수 제거. config.py:430 옛 주석 업데이트. 기존 저장된 system_state에 `_open_dir_cd` 키 잔존해도 restore 경로가 참조 안 하므로 무해 |
| 04-20 | T3 Defense TRAIL PnL 대폭 축소 (사용자 보고) | APT T3_DEF_TRAIL: 로그 -$0.64 vs 실제 -$22 (-5.24% ROI와 일치). 원인: order_router가 `ex.fetch_my_trades(limit=5)`로 realizedPnl 추출 → FORCE_CLOSE 대량 체결이 다수 조각으로 쪼개질 때 첫 5건만 합산 → 부분값. strategy_core가 `_rpnl != 0.0`이면 무조건 사용 → 부분값이 log_trade에 기록. 실측 오염 1건 확정(APT, ratio 2.9%). 메모리의 "V10.31b trim PnL 3배 뻥튀기"(트림 방향 과장)와 별개 버그 — 이번은 FC/TRAIL 방향 축소 | V10.31d-3: (a) order_router `fetch_my_trades` limit 5→50 (b) strategy_core에 검증 로직 — `abs(_rpnl) < abs(_self_pnl) * 0.5`이면 자체계산 `_self_pnl` 사용 + `[PNL_FIX]` 로그. realizedPnl 합리적 범위일 때만 그대로 사용(수수료·펀딩 반영된 정확값). 경고: runner.py limit fill 경로(TP1/TRIM)도 같은 패턴이지만 체결 조각 수 적어 부분값 위험 낮음 → 다음 세션에서 동일 적용 예정 |
| 04-20 | V10.31d 보강 로그 | 최초 배포 후 마이그레이션 함수가 조용히 early return (파일 없음 케이스) → "정말 실행됐는가?" 불확실. `_funding_fetch_loop` 복원 실패 시에도 조용히 last_ts_ms=0으로 시작 → 중복 fetch 가능성. 첫 주기 0건일 때 로그 자체가 없어 "fetch 작동 여부" 검증 불가 | V10.31d+: (a) `_migrate_log_trades_schema` 3분기(파일없음/빈줄/이미신규) 각각 명시 로그 (b) `_funding_fetch_loop` 복원 결과 4케이스 구분(파일없음/빈파일/파싱실패/정상복원) (c) FUNDING 첫 주기 0건 case 로그 추가 (d) 모든 신규 print에 `flush=True` (stdout 버퍼링으로 systemd 캡처 누락 방지) |
| 04-20 | 심볼 실적 기반 동적 조정 (Phase 3b) | Kim 판단: 기존 베타/corr 기반 선발은 미래 예측 신호. 후행지표인 실적을 소프트 반영해 (a) 지속 손실 심볼 임시 배제 (b) 선발 tiebreaker. 과적합 리스크 명시적 수용 (사용자 "거칠게 진행, 위험 감수") | V10.31e: 신규 `v9/strategy/symbol_stats.py` — `compute_symbol_stats()`(1h 캐시), `is_symbol_cooldown()`, `get_pnl_score()`. planners.py의 MR/TREND_COMP/TREND_NOSLOT 3곳에 쿨다운 체크 주입. universe_asym_v2 선발 랭킹에 `combined_score = atr_pct × (1 + 0.2 × pnl_score)` tiebreaker. status_writer/server에 심볼별 7일 실적 카드 + 🔴 CD 배지. 전체 flag `SYMBOL_STATS_ENABLED` — 문제시 즉시 원복. **한계 고지**: (1) 샘플 부족(n<5) 중립 처리했지만 현재 log_trades.csv 1일치만 존재 → 초기 1주일간 거의 동작 안 함 (2) PnL은 fee 차감한 net 사용 (V10.31d 활용) (3) 후행지표라 국면 전환 시 오판 가능 → 90일+ 누적 후 효과 재평가 필요 |
| 04-20 | T1 DCA 전 max_roi 측정 불가 (사용자 질문 "T1 DCA한 것 중 roi 1% 찍은 비율") | `runner.py:1514` 등 5곳에서 DCA 체결 시 `max_roi_seen = 0.0`으로 리셋 → T1 시점 반등 기록 소실. 청산 시 log_trade에 남는 max_roi_seen은 "DCA 이후 구간의 max"만 추적. 따라서 실측 1주일치 데이터로 T1→T2 DCA된 51건 분석해도 "T1에서 1%+ 찍었는지" 불가 (100% max_roi=0.0~0.5% 구간에 몰려 기록된 버그) | V10.31e 측정 인프라(**⚠️ 미작동, V10.31AD에서 실제 수정**): (1) DCA 경로 3곳(runner:1514, strategy_core:269/345)에 `max_roi_by_tier` dict로 tier별 max 보존 — 리셋 직전 `setdefault("max_roi_by_tier", {})[str(old_tier)] = old_max` (2) log_trade 호출부 3곳(runner trim/TP1_LIMIT, strategy_core FORCE_CLOSE)에서 `max_roi_by_tier["1"]` 추출해 `t1_max_roi_pre_dca` 파라미터 전달 (3) schemas.py에 컬럼 추가 (맨 뒤, index 19) (4) `_migrate_log_trades_schema` 확장 — `TRADES_COLUMNS` 전수 검사 방식으로 변경해 향후 컬럼 추가도 자동 백업/재생성. **로직 영향 제로**: 기존 리셋 타이밍·값 그대로 유지, 값만 dict에 보존. **TRIM 경로는 의도적으로 건드리지 않음** — trim은 성공 시점, 측정 관심 대상 아님. **측정 가능 시점**: 2주+ 누적 후 (n≥50 TRIM/FC 거래) 사용자 질문에 정확 답변 가능 |
| 04-24 | **V10.31e 측정 인프라 실제로 미작동 확인 (실측 12/12)** | 사용자 요청으로 04-24 로그 분석 중 발견. V10.31e 배포 후 누적된 log_trades.csv의 `t1_max_roi_pre_dca` 컬럼 전수 검사 — 최근 T2+ 청산 12건 **전부 0.0** ([실측] OP/ARB/XLM/AVAX/SUI/UNI/FIL/NEAR/OP/ETH/SUI/ADA). worst_roi_seen은 정상 기록(-1.9~-12.0) → 쓰기 경로 국한. **근본 원인 (compound bug 2개)**: (A) 쓰기 3곳(runner.py:1599, strategy_core.py:272, :351) 전부 `_pre_tier = int(p.get("dca_level", 1))`을 `p["dca_level"] = tier` 할당 **뒤에** 읽음 → NEW tier(예: 2) 읽혀서 `max_roi_by_tier["2"] = T1의_max` 저장. 리더는 모두 key `"1"` 조회 → 영구 miss. (B) strategy_core 같은 DCA 블록 안에 저장 블록이 2번(L270-275, L347-354) 있음. 두 번째 블록은 L274 `max_roi_seen=0` 리셋 후에 재실행 → 0.0으로 덮어쓰기. Bug A 고쳐도 B가 파괴. | V10.31AD: (a) runner.py DCA fill 핸들러 최상단에 `_pre_tier_val`/`_pre_max_val` 지역변수 캡처(L1532-1536, `p["dca_level"]` 할당 **이전**). 저장 시 이 지역변수 사용. (b) strategy_core.py DCA 블록 최상단도 동일 방식(L251 if 내부 맨 앞). (c) strategy_core.py 중복 저장 블록(L347-354) 완전 삭제 — 설명 주석만 남김. **검증**: AST 문법 PASS 3파일, 시뮬레이션 PASS(T1→T2 후 key "1" = 실제 T1 max 반환, T2→T3 후에도 "1"과 "2" 모두 보존). docs/DCA.md 함정/체크리스트 업데이트. **로직 영향**: 없음 (측정 값만 올바른 키에 저장). **데이터 영향**: 이번 배포 이후 청산된 T2+ 거래부터 `t1_max_roi_pre_dca` 실제 값 기록. V10.31e~AC 기간(04-20~04-24) 누적 데이터는 쓸모없음 — 사용자 원래 질문("T1 DCA한 것 중 ROI 1% 찍은 비율") 답변은 배포 후 2주+ 누적 필요. **교훈**: "수정 배포 = 수정 작동"이 아님. 측정 인프라 도입 시 배포 직후 실데이터 1~2건으로 end-to-end 검증(`cat log_trades.csv | awk '{print $20}'` 0 아닌지) 필수 규칙화 |
| 04-24 | **HEDGE_COMP 재비활성 (V10.31AC 롤백, 사용자 판단)** | 사용자 "니얼이랑 디오티가 mr인데 헷지 오프 시켜줘". 03-23 3시간 실현 집계 [실측]: MR 메인측(CORE_MR) HARD_SL 2건 LDO -19.82/OP -9.41 = **-29.23**, 헷지측(CORE_MR_HEDGE) trim 1건 LDO +0.63 = **+0.63**. 순 실현 -21.08, 잔고 변화 -43.61(미실현 포함). V10.31AC 재활성 근거(HEDGE_SIM 15건 100% TP1 + 실측 5건 4/5)는 유효했으나 실전 누적에서 MR FC 손실이 헷지 이익보다 구조적으로 크게 우세 (T1 HARD_SL 1건 ≈ 헷지 성공 20~30건 상쇄). 사용자 판단 우선. | V10.31AD: `config.py:29` HEDGE_COMP_ENABLED True → False. `planners.py:1162` early skip으로 신규 진입 차단. 이미 열린 CORE_MR_HEDGE 포지션(SUI/UNI 2건)은 기존 TP1/trim/trail 경로로 자연 청산. **다른 헷지 진입 경로 확인**: `plan_insurance_sh` 호출은 남아있으나 트리거 조건(BTC 급변 직접 감지)이 까다로워 로그 0건 [실측], `hedge_core.py`/`hedge_engine.py` 호출은 전부 관리/청산 로직(save/restore_hedge_state, check_hedge_tp1, plan_force_close)이지 진입 아님. 즉 `HEDGE_COMP_ENABLED` 단독 플래그가 유일한 진입 gate. **주의**: 플래그만 False로 두고 코드/문서/import 그대로 유지 — 향후 재활성 시 즉시 원복 가능. **검증 가능 시점**: 즉시 (배포 직후 신규 HEDGE_COMP 발사 0건 확인), 기존 2건 청산 완료 후 CORE_MR_HEDGE 포지션 0 유지 |
| 04-24 | **PTP arm=0.0 + drop=0.5%p 단일 tier + 쿨다운 1시간 (V10.31y 재도전, 사용자 A안)** | V10.31AD 배포 후 사용자 논리: "HEDGE/TREND 둘 다 off 상태에서 MR 단독으로 추세 방향으로 쏠리면 대책 없음. 시작하자마자든 이익 중이든 반대방향은 잘라야 함. 0.3은 과한가?" 분석 — 0.3%p는 MR 한 방향 정상 진행에서도 자주 찍힘(false positive 우려), 0.5%p가 평시 노이즈와 추세 전환 사이 구분점 [추정]. 사용자 "A안" 선택 = V10.31y 원복 + 쿨다운 안전장치. **V10.31y 재발 위험 고지**: V10.31y에서 arm=0.0 시도 시 TIA/SUI/ADA 대상 ReduceOnly -2022 대량 발생(17:08~17:12 수십 건). V10.31z에서 arm 0.3 롤백 + "step 0 진입 시 reduce preorder 전량 취소" 방어 구현했고 이 방어 로직은 그대로 유지됨. 단 취소는 비동기(`_TRIM_CANCEL_QUEUE`) — race window 여전 [추정]. 사용자가 "근본 접근"(방어 로직 신뢰) 선택. | V10.31AE: **(a) config.py** PTP_PEAK_TRIG_PCT 0.3 → 0.0, PTP_DROP_BY_PEAK 단일 tier `[(0.0, 0.5)]`, PTP_COOLDOWN_SEC=3600 신규 상수. **(b) planners.py `_ptp_update_state`** trigger_ts 체크 직후 + arming 체크 직전에 쿨다운 체크 블록 추가 — `now_ts < _ptp_cooldown_until`이면 return False. trigger_ts 세팅 지점에 `_ptp_cooldown_until = now_ts + PTP_COOLDOWN_SEC` 추가. **(c)** 자정 세션 리셋 블록은 trigger_ts/last_step만 pop, cooldown은 보존 (물리 시간 1시간 유효) — 기존 line 2543 그대로. **(d)** docs/PTP.md V10.31AE 섹션 신설. **실측 시뮬 PASS**: 04-23 19:12 start $3294.60 → 20:25경 $3278 미발동(0.5%p 직전) → 20:30경 $3275 발동 예상 → 단계적 청산 완료 시점에서 LDO FC(20:41) 대부분 방어 + OP FC(21:28)는 쿨다운 만료 후 재발동 가능 [추정]. **엣지 케이스**: (1) `_ptp_get_drop_thresh(0.0)` 단일 tier에서 정상 0.5 반환 확인 [단위테스트 PASS] (2) peak는 `max(session_start, current_balance)`라 항상 ≥ start → peak_gain 음수 불가 → `arm_pct=0.0` 조건 체크 무해 (3) step 3 완료 후 session_start/peak 리셋되지만 cooldown은 유지 → 2분 후 또 0.5% drop 시 **미발동** (쿨다운 기간). **한계 [필수 고지]**: (1) V10.31y baseline은 HEDGE 활성 시기 시뮬 — MR 단독 실거래 reaction 실측 필요 (2) noise trigger 빈도는 V10.31z 방어 + 쿨다운으로 제한되나 maker 체결률 따라 수수료 누수 변동 (3) 1주 모니터링 대상: `[PTP_TRIGGER]` 일평균 건수 / ReduceOnly -2022 재발 여부 / 발동당 방어 효과 vs 수수료. **검증 가능 시점**: 배포 직후 첫 `[PTP_TRIGGER]` 로그 확인 + 재발동 시도 시 쿨다운 로그 (return False silent) — 로그 추적 필요 시 쿨다운 분기에 debug print 추가 고려 |
| 04-24 | **일일 리포트/7일 히스토리/대시보드 집계에서 BC/CB 제외 (코어 전략 성과 분리)** | 사용자 "EC는 roi에서 제외해야지" (BC/CB 의미). BC(Beta Cycle), CB(Crash Bounce)는 MR과 별개 전략 + x1 레버리지. 기존 `_load_daily_pnl`, `_load_trade_stats`, `generate_daily_report`는 role 필터 없이 모든 trade PnL 집계 → BC/CB 실현 이익/손실이 MR 코어 성과에 섞여 순수 MR 전략 평가 오염. 현재 시점까진 BC/CB 실현 0건이라 실질 오염 0이지만 청산 시작하면 섞임 (예방적 수정). | V10.31AF: **telegram_engine.py 3곳 수정** (a) `_load_daily_pnl`: `role in ("BC","CB")` 행 스킵 — 7일 히스토리에서 BC/CB 제외 (b) `_load_trade_stats`: 동일 필터 — 대시보드 코어 라인에서 BC/CB 제외 (c) `generate_daily_report`: trades 로드 직후 `core_trades` / `sub_trades` 분리, 총 PnL/WR/avg/best/worst/Tier/Reason/Trim/hold 전부 `core_trades`만 사용 — Role 섹션(🎭)만 원본 `trades` 사용해 BC/CB 별도 라벨 유지. **설계 선택**: core만 있고 BC/CB만 있는 경우 대비 조기 리턴 블록 추가 (`if not core_trades`). **end-to-end 시뮬 PASS**: 코어 5건 + BC 3건 + CB 2건 샘플 → 총 PnL $+11.00(코어만, BC/CB 제외 확인) / 🎭 섹션 MR 4건 +$8.0 / BC 3건 +$0.0 / CB 2건 +$1.6 / Hedge 1건 +$3.0 모두 표시. **log_trades.csv 자체 무변경** — BC/CB 거래 기록은 그대로, 리포트 표시 레이어만 분리. docs/BC.md 하단에 V10.31AF 섹션 추가. **한계**: log_trades.csv 원본 접근하는 외부 도구/스크립트 있다면 그 쪽은 여전히 섞임 — 필요 시 각각 필터 적용 필요. **검증 가능 시점**: BC/CB 실현 발생 시 다음 날 00:05 UTC 자동 리포트 확인 — 메인 PnL은 코어만, 🎭 섹션에 BC/CB 건수 표시 |
| 04-24 | **SYNC vs pending_fill 이중 qty 반영 버그 — 메인 루프 순서 역전으로 구조적 해결** | 사용자 04-24 17:27 로그 분석 중 발견 [실측]: 08:21:03 FIL T2 DCA 체결 이벤트가 book에 이중 반영됨. SYNC가 먼저 qty 436→779.8 덮어씀(거래소 스냅샷 기준) → 같은 틱에 _apply_pending_fill이 +343.8 더함 → amt=1123.6 (의도 2배). 그 사이 TRIM_DBG_PF 로그에 amt=1123.6로 잘못된 qty 기반 trim 계산 수행. 30초 후 다음 SYNC cycle에서 779.8로 재조정되지만 중간 trim 계산 오염 + 직후 PTP 트리거와 겹치며 ReduceOnly -2022 대량 재발(15회+). 기존 V10.31r `_APPLIED_FILL_OIDS` 가드는 _apply_pending_fill 두 번 호출 차단 목적이라 본 케이스(SYNC + pending_fill 각 1회씩)는 통과. **근본 원인 구조 분석**: 동일 거래소 이벤트(DCA T2 체결)가 두 경로로 봇에 유입 — 경로 A `fetch_positions` → SYNC (snapshot 방식), 경로 B `fetch_order(oid)` → _apply_pending_fill (event 방식). 메인 루프(runner.py:3088/3092)가 SYNC 먼저 → pending_fill 나중 순서라 snapshot이 event보다 앞서 book을 선반영 → event가 추가로 또 반영. 두 관찰자의 역할 분리 부재가 구조적 결함. | V10.31AG: **runner.py 메인 루프 순서 역전 (L3085~3099)** — `_manage_pending_limits(pending_fill)` 을 `_sync_positions_with_exchange(SYNC)`보다 **먼저** 호출. **역할 위계 확정**: (1) Pending Fill = 1차 관찰자(event 기반, 정확도) → book 업데이트의 단일 진실 공급원 (2) SYNC = 2차 관찰자(snapshot 기반, 완결성) → pending이 놓친 고아 포지션 복구 + 검증 전용. **검증**: (a) FIL 시뮬 — pending_fill 먼저 +343.8 반영 amt=779.8 → SYNC는 book_qty=779.8 vs ex_qty=779.8 diff 0 → 추가 반영 없음 ✓ (b) 순수 고아 포지션(pending 없이 거래소에만 존재) → SYNC에서 여전히 복구됨 ✓ (c) WebSocket 체결 + pending + SYNC 3중 시나리오 → `_APPLIED_FILL_OIDS` 가드가 이중 반영 차단 ✓ (d) pending fill 일시 실패 → 다음 틱 SYNC에서 반영되므로 최종 수렴 ✓. **코드 변경 최소** — 메인 루프 2줄 위치만 스왑, 함수 내부 로직 무변경. AST 5파일 PASS. **관련 버그 히스토리**: V10.31r(ARB T3 04-22 _apply_pending_fill 중복 호출) 가드는 그대로 유효, 이번 수정과 상보. **한계 [필수 고지]**: 이번 수정은 runner 메인 루프 1곳만 바꾼 것 — order_router/position_book에 직접 접근하는 별도 경로(예: RECOVERED 복구 시점의 동기화 타이밍)에서 유사 race가 있는지는 미확인. docs/DCA.md에 V10.31AG 섹션 추가. **검증 가능 시점**: 배포 후 다음 DCA 체결 발생 시 — "SYNC qty 증가 감지" 로그가 안 찍히고(pending_fill이 먼저 반영했으므로 diff 0) 곧장 "PENDING_FILL DCA T2 반영"만 찍혀야 정상 |
| 04-24 | **PTP 자정 세션 리셋 완전 제거 — 진짜 무한 peak trail (사용자 S2안)** | 사용자 연쇄 논의: "일자로 안 끊고 쭉 가게" → "계속 트레일하다 0.5 떨어지면 청산" → "재시작은 상관없어야지". 구조 분석: (1) B1 "영구 누적 + 발동 후 peak 유지"는 drawdown 시 영구 청산 루프 → 기각 (2) B5 "peak 이월 + start 일별 갱신"도 여전 경계 있음 → 사용자 의도와 거리 (3) S2 "자정 완전 무시, peak 영구 upward only, 발동 완료 시 peak 리셋" 채택. 영구 청산 루프 방지는 step 3 완료 시 peak=current_balance 리셋 로직(기존 L2762~2765)이 담당. | V10.31AH: **planners.py `_ptp_update_state` L2508~2528 자정 블록 완전 제거**. `today_kst` 비교 / `_ptp_v31l_first_run_done` 마커 / `_load_today_balance_stats` 호출 모두 삭제. 대체: 최초 기동 시 1회 초기화 블록(`_ptp_peak_balance` 키 존재 여부로 판단) + `PTP_INIT` 로그. **재시작 persistence**: 별도 코드 불필요 — `system_state` 전체가 이미 `save_position_book`로 `v9_state.json`에 persist, `load_position_book`으로 자동 복원. 재시작 후 기존 peak/start/cooldown 그대로 연속. **시뮬레이션 4시나리오 PASS**: (1) 정상 Day1 흐름 peak 축적→0.5% drop 발동→쿨다운 ✓ (2) 자정 경계 KST 09:00 아무 이벤트 없이 peak 유지 ✓ (3) 재시작 시 save된 state 완전 복원 ✓ (4) step 3 완료→peak 리셋($3350→$3333)→쿨다운 중 미발동→쿨다운 후 peak $3333 대비 −0.51% 재발동 가능(영구 청산 루프 방지 작동) ✓. **Dead code 처리**: `_ptp_session_date_kst`/`_load_today_balance_stats` 함수 정의는 유지(Phase 4 sweep 시 제거 고려) — 향후 자정 경계 재도입 가능성 대비. **변경 파일**: `v9/strategy/planners.py` (자정 블록 삭제 + 초기화 블록 신설), `v9/config.py` VERSION, `docs/PTP.md` V10.31AH 섹션 신설. **부작용 [필수 고지]**: (1) 큰 상승 후 대규모 drop 시 단일 큰 청산 — 전엔 자정 리셋으로 분산됐지만 이제 한 번에 (2) 청산 규모 커지면 maker step 0~2 체결 어려워져 taker step 3 도달 빈도 증가 가능 [추정] (3) 1주 실측 필요 — step 3 도달률 vs maker 체결률 비교. **검증 가능 시점**: 배포 직후 `[PTP_INIT]` 로그 1회 발생 + 자정 경계에서 `[PTP_SESSION_*]` 로그 안 찍힘 + 재시작 후 `v9_state.json`에 peak/cooldown 유지 확인 |
| 04-24 | **BC/CB x1 ROI 일관 반영 + PTP 대시보드 AH 동기화** | (Part 1) 사용자 "데시보드랑 텔레그램에 3배로 표시됨" — BC/CB는 Binance에서 x1 레버리지로 체결되는데 ROI 계산은 `calc_roi_pct(..., LEVERAGE=3)` 하드코드라 3배 뻥튀기. 실증 [실측]: DYM BC 실거래 기록 `roi=-27.785%`, 실제 가격 변화는 `-9.262%` (정확히 3배). 오염 범위: `log_trades.csv` roi_pct 컬럼 + `status_writer.py` 대시보드 표시. (이미 올바른 곳: `logger_ml.py`, `telegram_engine.py` 두 위치). V10.31AF BC/CB 필터가 일일 리포트에서 제외하지만 개별 포지션 표시 및 log_trades 원본 자체는 여전 오염. (Part 2) 사용자 "데시보드 1% 도달시 트레일" 표시와 실제 설정(AE arm=0%, AH 자정 리셋 제거) 불일치. `status_writer._build_ptp_status` docstring/threshold/import 3곳 구식 상태. | V10.31AI Part 1: **helper 신설** `v9/utils/utils_math.py`에 `role_leverage(role)` (BC/CB=1, 나머지=LEVERAGE), `calc_roi_pct_by_role(ep,cp,side,role)` 추가. **5곳 수정** — BC/CB가 실제 거치는 경로만 선별: (a) `strategy_core.py:422` TP1_LIMIT_FULL log_trade (b) `strategy_core.py:580` close/FC log_trade (c) `runner.py:1720` trim 표시 (d) `runner.py:1773` 전량체결 log_trade (e) `runner.py:1823` 부분체결 log_trade (f) `status_writer.py:276` 대시보드 roi/pnl_est. **손대지 않은 곳**: planners.py heavy_rois/SL 체크, hedge_core/hedge_engine, strategy_core hedge 경로 등 30+ 곳 — MR/HEDGE 전용이라 BC/CB 도달 불가. V10.31AI Part 2: `status_writer.py._build_ptp_status` — (g) docstring "peak_gain < 1%" → 동적 `PTP_PEAK_TRIG_PCT` 반영 (h) L230 `peak_gain_pct >= 1.0` 하드코드 → 동적 임계 (AH arm=0 반영 = 항상 armed) (i) import 경로 `from v9.config import _ptp_get_drop_thresh` — config와 planners 둘 다 동일 함수 있지만 config 버전으로 통일 (j) 신규 필드 `arm_trig_pct`, `cooldown_remaining_sec` 추가 (k) `state="cooldown"` 신규 분류. **실측 검증 PASS**: DYM BC 실거래 `-27.785%` → 수정 후 `-9.262%` (실제 가격 변화 일치). PTP 4상태(idle/armed/cooldown/active) 전부 정상 동작. **과거 log_trades.csv 오염 데이터**: 수정 불가 (이미 기록됨) — 하지만 V10.31AF BC/CB 제외 필터로 리포트 노출은 막혀있음. 신규 BC/CB 청산부터 정확한 roi 기록. **영향 없음**: MR/HEDGE 거래 ROI 계산은 기존 그대로 유지(lev=3). **검증 가능 시점**: (1) 대시보드 개별 BC 포지션 ROI가 3배 덜 표시되는지 즉시 확인 가능 (2) 다음 BC/CB 청산 시 `log_trades.csv` roi_pct 컬럼이 가격변화 x1과 일치하는지 확인. **한계**: telegram_bot.py 쪽 웹 대시보드에서 PTP 표시하는 별도 코드 있다면 그쪽은 status_writer JSON을 소비하므로 자동 반영됨 — 별도 수정 불필요 |
| 04-24 | **PTP 활성 중 trim/tp1/dca preorder 재생성 차단 (ReduceOnly -2022 근본 해결)** | 사용자 04-24 20:26 텔레그램 알림 `⚠️ TRIM 실패 INJ T2` 9회 연속 보고. 로그 분석 [실측]: 11:17:25 PTP_TRIGGER (peak=0.11%, drop=0.53%p) → PTP_S0 INJ buy limit 배치 → V10.31z 방어 로직 작동(기존 TRIM preorder 4개 취소). 그러나 11:18~11:22 4분간 `plan_portfolio_tp`가 `cur_step <= last_step` 조건에 걸려 `return []` — step 발사는 안 하지만 `return intents` 조기 종료도 안 함. 이 gap에 `plan_trim_trail`이 INJ T2 포지션 발견 → trim preorder 재생성 → 거래소에 "PTP buy limit(reduce) + TRIM buy limit(reduce)" 동시 존재 → 두 reduce qty 합이 포지션 초과 → Binance `-2022 ReduceOnly Order is rejected`. 재시도 3/3 실패 → 포기 → 텔레그램 알림. 10초 주기로 반복. **근본 원인**: V10.31z 방어 로직은 "PTP step 0 진입 시 **1회** 기존 preorder 취소"만 처리. **trigger 활성 상태 동안 재생성 차단 안 함**. step 사이 간격(5분 × 3단계)에 trim/tp1/dca planner가 정상 로직으로 재생성 → 경쟁. | V10.31AJ: **PTP trigger 활성 심볼 추적 시스템 도입**. (a) `plan_portfolio_tp`에서 `_ptp_update_state`가 True 반환한 순간부터 `system_state["_ptp_active_syms"]`에 청산 대상 포지션 심볼 set 저장 — BC/CB/INSURANCE_SH/SOFT_HEDGE/CORE_HEDGE 제외한 모든 활성 포지션. step 발사 타이밍과 무관하게 trigger 활성 동안 매 틱 재세팅 (step gap 4~5분 공백 방지). (b) `generate_all_intents` `_exclude` set에 `_ptp_active_syms` 포함 → `plan_tp1`/`plan_trim_trail`이 해당 심볼 스킵. (c) runner 3개 preorder 함수 시그니처에 `system_state=None` 추가 — `_manage_tp1_preorders`/`_place_trim_preorders`/`_place_dca_preorders`. 각 함수 내부 for 루프 시작에 `if sym in _ptp_active: continue` 가드. (d) 메인 루프 호출부 3개에 `system_state=system_state` 전달. (e) step 3 완료 시 `system_state.pop("_ptp_active_syms", None)` — preorder 정상 처리 재개. **시뮬 검증 PASS**: 04-24 11:17 시나리오 재현 — V10.31AJ 적용 시 INJ/FET/UNI가 `_ptp_active_syms`에 세팅되어 plan_trim_trail/\_place_trim_preorders 모두 스킵 → trim 재생성 0건 → -2022 발생 없음 ✓. BC(RUNE) 제외 확인 ✓. step 3 완료 후 다음 틱부터 정상 처리 재개 ✓. **영향 없음**: 정상 상황(PTP 미활성)에서는 `_ptp_active_syms`가 빈 set이라 모든 preorder/intent 정상 작동. **관련 버그 히스토리**: V10.31z(step 0 진입 시 1회 취소) 그대로 유지 — AJ는 step 0 이후 step 1~2 공백 보완. **변경 파일**: `v9/strategy/planners.py` (2곳: _ptp_active_syms 세팅/pop), `v9/app/runner.py` (4곳: 3 함수 시그니처 + 3 내부 가드 + 메인 루프 호출부), `v9/config.py` VERSION → AJ. AST 7파일 PASS. **한계 [필수 고지]**: (1) `_ptp_active_syms` 동기화 타이밍 — `plan_portfolio_tp`는 매 틱 실행되므로 실시간 유지되지만, 봇 재시작 직후 첫 틱 전까지 공백 가능 → 재시작 후 PTP가 이미 trigger 상태면 첫 틱까지 trim 재생성 가능(극히 짧은 window) (2) BC/CB 포지션은 세트에서 제외 — BC 전용 trim 로직이 있다면 영향 없음 확인 필요 [추정, 현재 로직상 BC는 HARD_SL/자체 SL만 사용하여 trim 경로 없음]. **검증 가능 시점**: 배포 후 다음 PTP_TRIGGER 발생 시 — 알림 이후 5~15분 동안 `TRIM 실패` 알림 0건이면 수정 성공 |
| 04-20 | 429 Too Many Requests 반복 발생 | 04-20 04:08~04:17 8회 실측. 원인 분해: `fetch_tickers()` weight 40 × 메인 루프 1초 × 60회/분 = **2400** (Binance IP 한도 정확히 근접). `fetch_ohlcv` 16심볼 × 4TF × 6회/분(10s 주기) = **1920 weight/분**. 합계 4350 weight/분 → 한도 2배 초과. Universe refresh(5분) 시 33심볼 × 1h ohlcv 폭주로 순간 초과 → SYNC/snapshot 실패 연쇄. 진입 판단 자체 불가 구간 발생 | V10.31e-3: (1) `_TICKERS_CACHE` 3초 TTL 도입 → tickers 2400→800 weight/분 (2) `ohlcv_interval_sec` 기본값 10→15s → 1920→1280 weight/분. 합계 4350→2110 (한도 2400 대비 290 여유). Universe refresh 피크도 소화 가능. **trade-off**: (a) 티커 가격 최대 3초 지연 — 5m 봉 전략에 무해 (b) ohlcv 5초 지연 — 1m 지표(RSI/micro)에 약간 영향이나 체결은 5m close 기준이라 무해. 캐시 fallback 체인: `_TICKERS_CACHE["data"]` → `prev_snapshot.tickers` → 빈 dict. **재분석 결과**: `fetch_positions` 429는 30초 주기로 자체 부하 낮음, tickers 폭주에 휩쓸린 피해자 → 이번 조치로 자동 해결 [추정]. 다음 세션 검증 필요 |
| 04-20 | Falling Knife 필터가 MR 철학과 충돌 (사용자 지적) | v9.9부터 관성으로 유지된 "최근 3×5m 봉 누적 ±2% 이상이면 MR 진입 차단" 로직. MR = "이격 구간 반대 진입"인데 Falling Knife = "이격 커지면 차단" → 서로 모순. 9일 실측 분석 결과: 필터 있는 MR T3 FC 5.8% (n=52) vs 필터 없는 TREND T3 FC 4.4% (n=136). 필터가 오히려 T3 FC 비율 높이고 기회만 봉쇄 확인. 사용자 진단 "물린 구간 반대 진입이 들어와야 하는데 쟤가 막는 거네" 정확히 적중 | V10.31e-4: (1) `planners.py:843-847` 호출 제거 (2) `_is_falling_knife_long/short` 함수 정의 제거 (3) `FALLING_KNIFE_BARS/THRESHOLD` import 제거 (4) config.py 상수는 참조 없으나 롤백 포인트로 유지하며 "미사용" 주석 (5) docs/OPEN.md 조건 6번 제거 표기. **기대 효과** [추정]: MR 진입 시도 증가 → TREND 편중 완화. 실측 검증은 1주+ 누적 후 entry_type=MR 비율 확인 |
| 04-20 | **418 I'm a teapot — IP 밴 550분** (긴급) | V10.31e-3 배포 전 기존 코드로 운영 중 fetch_tickers 폭주 누적 → 429 반복 → Binance 418 DDoS 차단. 밴 지속 ~9시간 20분. 재발 시 시스템 전체 마비. 이전 V10.31e-3의 weight 2110/분도 한도 2400 대비 여유 290밖에 없어 안전마진 부족 | V10.31e-5 압력 최소 하드닝 전체 적용: (1) tickers TTL 3s→5s (분당 20→12회, weight 800→480) (2) ohlcv_interval 15s→30s (weight 1280→128 — 이전 계산 과대였음, 실제 weight 1) (3) 메인 루프 1s→2s (모든 API 빈도 절반) (4) Universe refresh 5min→15min (ohlcv 33×1h 폭주 빈도 1/3) (5) universe_asym_v2가 market_snapshot의 `_TICKERS_CACHE` 공유 (중복 fetch 제거). **실측 weight**: tickers 480 + ohlcv 128 + balance 20 + positions 10 + universe 14 + my_trades 25 = **677 weight/분** (한도 2400의 28%, 여유 72%). **trade-off**: 가격 지연 최대 5초, ohlcv 지연 최대 30초, 심볼 회전 15분 단위. 5m 봉 전략이라 진입 판단에 무해. trim/trail 반응 2초 주기로 느려짐 — 체결 타이밍 손실은 무시할 수준. **롤백 포인트**: 각 수정 지점에 V10.31e-5 주석. 1~2주 정상 운영 확인 후 약간 타이트하게 조정 가능 (tickers 4s, ohlcv 20s 등) |
| 04-20 | 418 I'm a teapot — IP 차단 발생 | 04-20 ~04:40 KST (05:10 UTC) 차단. 메시지: "banned until 1776661799909" (9시간). V10.31e-3에서 캐시 추가했지만 **사용자 서버에 미배포 상태**라 구버전 기존 폭주 그대로 진행 → 418 밴. 밴 발생 후 봇이 계속 두드리면 연장 위험. weight 계산 재점검: `fetch_tickers` weight 40×60회=2400 단독으로 한도 전부 소진 | V10.31e-5 다중 하드닝: (1) 메인 루프 1→2s (모든 API 호출 빈도 절반) (2) tickers 캐시 3→5s (3) ohlcv 15→30s (4) universe 5→15min (5) ccxt `rateLimit` 50→100ms (개별 호출 간격 보장) (6) **418 자동 감지+회복**: market_snapshot이 418 감지 시 "banned until" ms 파싱해 `/tmp/trinity_ban_until.txt` 저장 → 메인 루프가 read해서 해제까지 60초 간격 슬립 (API 호출 전혀 안 함, 밴 연장 방지). 해제 30초 전부터 플래그 삭제 후 정상 복귀. 예상 weight: ~750/분 (한도 2400의 31%). 418 재발 시 폭주→장기 밴 순환 근본 차단 |
| 04-20 | HEDGE_SIM 중간형 DCA 시뮬 확장 (사용자 요구) | 기존 HEDGE_SIM (v10.29e)은 MR T1 진입가만 기록 → 청산 시 단순 ROI 비교. 실전 MR은 DCA까지 물리는데 시뮬은 반영 안 해 **과소평가**. 사용자 요구: "트렌드 진입 시점에 MR 헷지 동시 진입 (반대 방향) + DCA까지 포함한 가격 추격 병렬 시뮬". Q1 MR 시그널 심볼 / Q2 TREND 동일 notional / Q3 독립 종료(TP1 2% / HARD_SL -10%) | V10.31e-6: (1) `schemas.py` `HEDGE_SIM_COLUMNS` 14개 추가 (2) `logger_csv.py` `log_hedge_sim()` 함수 (3) `planners.py:1187~` HEDGE_SIM 기록 시점에 시뮬 필드 확장 — tier/blended_ep/t2_notional/t3_notional/DCA 트리거 맵/TP1/HARD_SL 임계 (4) `runner.py` 신규 `_tick_hedge_sim()` 매 틱 호출 — ROI 계산, DCA 트리거 시 평단 압축, 종료 조건 도달 시 log_hedge_sim 기록 (5) 메인 루프에 `_tick_hedge_sim(system_state, snapshot)` 1줄 삽입 (6) docs/TREND.md에 설명 추가. **실전 영향 0**: 읽기 전용 + 자체 state + try/except. 시뮬 파라미터는 실전 config 동일 (`DCA_WEIGHTS=[33,33,34]`, `DCA_ENTRY_ROI_BY_TIER={2:-1.8,3:-3.6}`, `TP1_FIXED[1]=2.0%`, `HARD_SL_BY_TIER[3]=-10%`). **한계** [필수 고지]: (a) 가상 MR은 "이상적 조건" 가정 — 실제 MR 게이트(BB/RSI/VS/corr) 통과 필요해 재현 불가 (b) 샘플 쌓이는 속도 ≈ TREND_COMP 발사 빈도 (c) LEVERAGE 동일 가정, 수수료·펀딩 미반영. **집계 가능 시점**: 2주+ 후 n≥30 시뮬 종료 데이터로 "TREND_COMP 전략 vs 가상 반대진입" 비교 |
| 04-20 | V10.31e-6 UnboundLocalError 긴급 (내 실수) | 내가 V10.31e-6 배포한 직후 실전에서 `cannot access local variable 'DCA_WEIGHTS' where it is not associated with a value` 크래시 반복 발생. 원인: `planners.py:1190`에 `from v9.config import DCA_WEIGHTS, ...`를 HEDGE_SIM 기록 블록에 넣었는데, `DCA_WEIGHTS`는 이미 module-level(line 258)에 import되어 있음. Python 스코핑 룰: **함수 내부에서 이름을 한 번이라도 대입/import하면 그 이름은 함수 전체에서 로컬 변수로 처리**. 결과: 같은 함수 line 1166 `_tr_notional = _tr_grid * (DCA_WEIGHTS[0] / sum(DCA_WEIGHTS))`에서 "local이지만 할당 전"으로 크래시. ast.parse는 문법만 체크 → 스코프 에러 검출 불가. 단위 테스트 누락 | V10.31e-7 핫픽스: (1) `TP1_FIXED`, `HARD_SL_BY_TIER`를 module-level import(line 258~273)에 추가 (2) 함수 내부 로컬 재import 블록 제거 (3) AST 레벨 DCA_WEIGHTS 로컬화 탐지 테스트 추가 — 향후 회귀 방지. **교훈**: 이미 module-level에 있는 이름은 함수 내부에서 재import하면 안 됨. 필요하면 module-level에 추가하거나 별명으로 import |
| 04-20 | ETH TRIM 무한 실패 루프 (사용자 실전 경고 10건) | 증상: `⚠️ TRIM 실패 ETH T3 binance amount of ETH/USDT:USDT must be greater than minimum amount precision of` 반복. 원인: ETH amt=0.359(T2 사이즈)+tier=3 모순 상태(앞선 sync 버그 후폭풍)에서 `calc_trim_qty`가 극소값(또는 0) 반환 → Binance LOT_SIZE 필터 탈락 → 3회 재시도 실패 → 텔레그램 경고 → 다음 틱 regen 재발동 → 무한 루프. `_place_trim_preorders` 시작부 `trim_qty < min_qty` 체크(line 2158)는 있지만, **regen 경로는 이 체크 전에** trim_to_place 세팅 완료 후 다음 틱에 도달 | V10.31e-7 수정: regen 블록 line 2133에 **Binance LOT_SIZE 필터 사전 방어** 추가 — `_regen_qty < SYM_MIN_QTY` 시 `[TRIM_REGEN_SKIP]` 로그 + continue, trim_to_place 세팅 안 함. 근본 해결은 sync 로직에서 amt-tier 정합성 회복해야 함(다음 세션 과제). 지금은 **폭주 자체 차단**으로 텔레그램 스팸 + 불필요 재시도 방지. 사용자는 ETH 수동 청산(+0.79% 익절)으로 모순 상태 해소 권장 |
| 04-20 | PRE_MKT Phase 1 단일 배치 → 10분 단위 프리미엄 양보 (사용자 요구) | 기존(V10.31b) Phase 1 로직: ET 08:00에 **한 번만** +0.5% limit 배치하고 30분 대기 후 Phase 2 시장가. 사용자 관찰: "시장이 반대로 움직이면 +0.5% 고정은 체결 안 됨 → 결국 Phase 2 시장가 가야 해서 의미 없음". 사용자 요구: "30분 전부터 0.5%에서 10분마다 양보해 2번 더 내릴 수 있지, 깔끔하게 선주문으로". 관련 .md 없음 — 기존 로직이 문서화 안 된 상태 | V10.31e-8: (1) `planners.py:1876~` Phase 1 재작성 — `_step = (et_min - 480) // 10` (0/1/2) 기준 프리미엄 `0.5% → 0.35% → 0.20%` 단계별 재배치 (2) 스텝별 키 `_pmc_p1_{date}_s{step}` 중복 방지 (3) **각 스텝 시작 시 `is_pre_market_limit=True` 태그 달린 이전 limit 주문 전부 `_TRIM_CANCEL_QUEUE` 추가**로 취소 → 새 프리미엄 재배치 (4) `order_router._register_pending_limit`에 `is_pre_market_limit` 필드 저장 (5) 방향성 검증: 롱→sell@curr×(1+p), 숏→buy@curr×(1-p) = 유리한 방향 (6) DCA 선주문 취소는 첫 스텝(08:00)에만 실행해 중복 방지. 문서화: docs/PRE_MKT.md 다음 세션에 추가 |
| 04-20 | T3 8h 컷 도입 (사용자 설계 확정) | 실측: T3 + hold>=8h = 25건 -$519 (14일). T1/T2는 TRIM/TP1이 잘 처리 (+$56). 사용자 요구: "8시간 컷, 7시간부터 지정가로 서서히 압박하고 8시간되면 시장가 컷". 설계 질문에 대한 답: 대상 T3만 / 조건 없이 모든 8h+ (일관성) / 시작 프리미엄 0.5%. T3_DEF(방어 모드)와 충돌 시 T3_DEF 우회하고 일괄 정리 (사용자 일관성 우선 선택) | V10.31f: (1) 신규 `plan_t3_8h_cut` 함수 (`planners.py` 끝) — 7h00 step0(+0.5%) / 7h20 step1(+0.35%) / 7h40 step2(+0.20%) / 8h00 step3(시장가). 포지션별 `_t3_8h_step` 필드로 단계 추적, 중복 방지. (2) `generate_all_intents`에 PMC 다음 우선순위로 호출 + exclude_syms에 추가 (3) `order_router._register_pending_limit`에 `is_t3_8h_limit` 필드 저장 — 단계 진입 시 이전 limit 취소 추적용 (4) DCA 선주문은 step 0에서만 취소 (5) open_ts(`p["time"]`) 없으면 skip 방어 (6) 기존 PRE_MKT 로직 건들지 않음. docs/T3_8H.md 신규. **예상 효과**: 보수 +$200~300/14일, 2주 관찰 후 재평가 |
| 04-21 | T3 trim HIGH 레짐 trail 조기 이탈 패턴 (사용자 판단) | 사용자 체감: "TREND가 메인이 됐고 T3 FC 손실 이어진다". 코드 분석으로 plan_trim_trail의 T3 처리 검토 — T3 trim threshold +0.5% (매우 작음) + HIGH 변동성에서 peak +0.5% 찍은 후 trail gap 0.3% 하회만으로 발동 → +0.2%만 먹고 이탈 → 다시 T3 복귀 → T3_DEF/PRE_MKT/T3_8H 강제 청산으로 끌려가는 패턴. 사용자 판단: "T3은 하이레짐때도 그냥 지정가 유지로 하자" — LIMIT은 +0.5% 정확 도달 시 maker(0.02%) 수수료로 깔끔히 T2 감소, 미체결 시 그대로 대기 (trail이 0.2%에서 이탈하는 것보다 보수적이면서 ROI 보존도 좋음). **약점 [필수 고지]**: HIGH에서 T3이 +0.5% 넘어 +2~3% 급등할 때 LIMIT은 +0.5%에서만 trim 체결 (잔량 +2%까지 trail은 이어짐) — 기회손실 제한적. 200줄 로그 기준으로는 "HIGH trail이 T3 FC 직접 원인" [실측] 근거 약함, 2주+ 관찰 필요 [추정] | V10.31g: (a) `planners.py:1495` plan_trim_trail 진입부 분기에 `or dca_level >= 3` 추가 — T3은 레짐 불문 trail 경로 skip + 잔존 trail 상태 자동 정리 (b) `runner.py:2055` _place_trim_preorders HIGH 분기에 `_dca_level_p < 3` 추가 — T3은 HIGH라도 선주문 fall through (regen 로직 그대로 작동) (c) docs/TRIM.md 레짐 분기표 + 체크리스트 갱신 (d) **로직 영향 범위**: T2 동작은 완전 동일 / T3은 LOW/NORMAL/HIGH 모두 동일 LIMIT 경로 / T3_DEF·T3_8H·PRE_MKT는 독립 경로라 영향 없음 |
| 04-21 | TREND_NOSLOT 누적 양산 패턴 + universe 강제분류 1순위 필터 검증 (사용자 분석) | 사용자 체감 "MR이 메인인데 TREND가 주가 됐다" 인과 추적: 04/20 14~15시 NOSLOT 13건 중 TIA 시그널 단독 5건 연쇄(14:40~15:08), AVAX 시그널 단독 4건 연쇄(07:25~07:26). 시뮬: 모든 NOSLOT이 시그널 방향 슬롯 풀 상태에서 발사됨 → 트리거 자체는 사용자 의도 충족. **진짜 양산 원인**: 1회 NOSLOT 발사로 비대칭 해소된 후에도 새 시그널 오면 또 발사 → 결국 양쪽 슬롯 다 채워짐. 추가 분석으로 universe 안정성 확인 — 200회 갱신 중 100% 일관 심볼이 9/22(LONG: AVAX/XLM/SOL/XRP/BNB, SHORT: FET/OP/INJ/WLD/FIL/TIA/SEI/ATOM/ARB) → 펀딩비 알파 안정. 사용자 결정: **(1) NOSLOT A 조건만**(Option 2 universe 존중은 보류, COMP는 미수정 유지). **(2) 시나리오 A로 LONG_ONLY/SHORT_ONLY 축소**: LONG 8→4(BNB/XRP/XLM/AVAX), SHORT 14→6(TIA/FET/OP/INJ/WLD/FIL). **(3) MAJOR_UNIVERSE 정리**: 200회 0회 등장 14개 제거(TRX/TON/ICP/ETC/AAVE/STX/MATIC/EOS/STRK/RUNE/RNDR/AGIX/AKT/GRT) + 7개 추가(LDO/PENDLE/JUP/JTO/ARKM/GMX/ORDI). 결과 36→29개 | V10.31h: (a) `planners.py:892~` NOSLOT 진입 분기에 A 조건 — `_a_ok = (_tr_opp_slots + 1) < _sig_side_slots`. 위반 시 5분 cooldown 로그 `[NOSLOT_SKIP_A]` (모듈 dict `_TREND_SKIP_LOG_CD` 재사용). (b) `config.py:244~` LONG_ONLY/SHORT_ONLY 축소 + NEUTRAL 재구성. ARB/SEI/ATOM은 SHORT_ONLY→NEUTRAL 이동(등장 확인됨), 14개 제거(0회 등장)/7개 추가(메이저 알트 미커버 영역). MAJOR_UNIVERSE 자동 합집합. (c) docs/UNIVERSE.md 분류 갱신, docs/OPEN.md NOSLOT A 조건 추가. **로직 영향**: COMP는 무수정(사용자 지시), MR 진입 풀 자체는 변동 — TREND/MR 모두 새 universe 따름. **부작용 [필수 고지]**: 펀딩 알파 강한 100% 일관 심볼은 강제 분류로도 자연 분리로도 같은 결과(redundant 제거). NEUTRAL 확대로 같은 심볼이 양 풀 모두 들어갈 가능성↑ → MR 양방향 평가 자유도↑. 추가 7종은 거래량 floor 초기 미달 가능 — 1주일 관찰 후 등장 빈도 확인 필요. **검증 가능 시점**: 1주+ 누적 후 (1) NOSLOT 발사 빈도 13/2h → 추정 2~3/2h 감소 (2) universe 등장 신규 7종 분포 (3) 제거 14종 ohlcv_pool 잔존 0 확인 |
| 04-21 | TREND_COMP 스큐 예방 가드 도입 (사용자 프레임 정의) | 사용자 프레임: **"COMP는 스큐 예방 목적, 주 수입원 아님"**. 이 프레임이 설계 기준을 결정 — 가드는 "스큐 발생/심화 케이스에서만 발사"가 본질. NOSLOT A 조건을 그대로 이식 불가: NOSLOT은 단독 발사(opp만 +1, 비대칭 해소)이지만 COMP는 MR과 동시 발사(양쪽 +1, 비대칭 절대값 불변). 즉 COMP는 A 조건이 막으려는 "비대칭 역전" 자체를 일으키지 않음. 200줄 로그 분석: OPEN 5건 중 COMP 2건(40%), MR 진입 2건 모두에 COMP 동반 → 슬롯 점유 2배 가속 [추정]. 04/20 18:30~21:08 sig=sell sig_slots=4 opp=3 상태 4시간+ 지속(NOSLOT_SKIP_A 로그) — 일부는 COMP 누적 기여 가능성. 사용자 확정: "MR보다 하나 적게" 유지 = 발사 후 opp<sig. 균형/opp우세 상태는 **신규 스큐 생성을 MR에 전담**시키고 COMP는 양보 | V10.31i: (a) `planners.py:1109~1135` COMP 분기에 스큐 가드 — `_comp_skew_ok = _tr_opp_slots < _sig_side_slots`. 수식 도출: 발사 후 opp+1 < sig+1 = opp < sig. 기존 `_tr_opp_slots >= MAX_MR_PER_SIDE` 차단 유지, elif로 추가. 위반 시 5분 cooldown 로그 `[COMP_SKIP_SKEW]` (`_TREND_SKIP_LOG_CD` 재사용). (b) docs/OPEN.md TREND_COMP 섹션에 가드 추가 — NOSLOT A 조건과 수식 차이 명시. (c) docs/TREND.md 함정/체크리스트 갱신 — NOSLOT(`opp+1<sig`)과 COMP(`opp<sig`) 기준선 차이 설명. **엣지 케이스 검증**: 0/0/1/1/2/2/3/3 균형상태 전부 차단, 3/1/3/2 스큐상태 발사, 1/3 역방향 차단. **기대 효과** [추정]: COMP 발사 빈도 30~50% 감소, 슬롯 점유 가속 완화, 후속 MR 진입 여지 확보. **한계 [필수 고지]**: 200줄 로그 n=2로 COMP 양산 패턴 자체는 [실측] 미확인 — NOSLOT A와 달리 명확한 [실측] 트리거 없음. 단 사용자 프레임("스큐 예방")과 로직 일치는 데이터 없이도 성립. **검증 가능 시점**: 1주+ 누적 후 (1) COMP 발사 빈도 변화 (2) entry_type별 PnL/WR (3) 슬롯 풀 도달 빈도 감소 |
| 04-21 | **worst_roi 기반 동적 TRIM 임계 + entry_type별 T3 시간 컷 + PRE_MKT 주석처리** (사용자 종합 설계) | 사용자 진단: "TREND가 메인이 됐고 T3 FC가 주 손실원". 실측 검증 (OLD 500건): TREND_T3 FC 17건 -$513 vs TREND_T3 TRIM 29건 +$173 (hold <3h 회복률 90%, ≥3h 38%). MR_T3 FC 8건 -$192는 전량 >12h 패턴 (시간 누적). 즉 **entry_type별 FC 메커니즘 상이** — TREND=빠른 급락 / MR=장시간 물림. T2_DEF(worst≤-2→TRIM 1.5→0.5)/T3_DEF_M5(worst≤-5→TRIM 0.5→-0.5)는 실측 효과 제한적이나 **데이터 축적 목적(worst_roi_seen 로깅)과 일관성**으로 도입. 사용자 추가 결정: PRE_MKT(미장전 ET 08:00~08:30 정리) 주석처리(기능 보류, 함수 정의 유지). **추가 고려했다 기각**: 전체 ROI+마진율 AND 전체청산 (마진율 30% 사실상 항상 참 + 수수료 85% 잠식 + TP1 시스템 무력화). **추가 고려했다 기각**: T3_DEF `2×worst+13` 공식 즉시 활성 (worst 이력 로깅 없어 튜닝 불가) | V10.31j: **(a) config.py** — T2_DEF_WORST_ENTER=-2.0 / T2_DEF_TRIM_THRESH=0.5 / T3_DEF_M5_WORST_ENTER=-5.0 / T3_DEF_M5_TRIM_THRESH=-0.5 상수 + `calc_dynamic_trim_thresh(tier, worst_roi)` 함수 + `calc_trim_price()` 시그니처에 `worst_roi=0.0` 인자 (기본값이라 기존 호출부 무영향). **(b) planners.py** — `plan_trim_trail` 임계 동적화, `plan_t3_8h_cut`에 `entry_type=="MR"` 조건 추가, 신규 `plan_t3_3h_cut_trend` (TREND only, 3h00/3h20/3h40/4h00 단계, `_t3_3h_step` 필드 + `is_t3_3h_limit` 플래그로 T3_8H와 완전 분리), `generate_all_intents`에 plan_t3_3h_cut_trend 호출 추가, PRE_MKT 호출 3곳 주석처리(line 511 진입차단 체크 + line 2409 plan_pre_market_clear 호출 + line 2436 NOSLOT 발사 차단). **(c) runner.py** — `_place_trim_preorders` EP 검증/regen 블록에 `worst_roi` 전달 (기존 EP 변경 재배치 로직에 편승 — worst 구간 전환 시 `_v_correct` 가격 변동으로 자동 취소+재배치), worst_roi 매틱 갱신 지점에 `[T2_DEF_ENTER]`/`[T3_DEF_M5_ENTER]` 이벤트 로그 추가 (포지션당 1회, `_t2_def_logged`/`_t3_def_m5_logged` 플래그), DCA 체결 시 두 플래그 리셋, `_migrate_log_trades_schema`에 pre_v10_31j 백업 태그, log_trade 호출부 3곳(TRIM/TP1_LIMIT_FULL/strategy_core 범용)에 `worst_roi_seen` 전달. **(d) schemas.py** — TRADES_COLUMNS 21번째에 `worst_roi_seen` 추가. **(e) logger_csv.py** — `log_trade` 시그니처 + row 확장. **(f) strategy_core.py** — FORCE_CLOSE log_trade에 worst_roi_seen. **(g) docs/TRIM.md** — V10.31j 동적 임계 섹션 신설, TP Trail 흐름표에 디펜스 동작 명시, 체크리스트 3항목 추가. **(h) docs/TREND.md** — T3 시간 컷 테이블(TREND 3h-4h vs MR 7h-8h), 체크리스트 4항목 추가. **(i) docs/DCA.md** — DCA 체결 시 필수 클리어 항목에 2개 플래그 추가. **엣지 케이스 처리**: (1) T2→T3 DCA 진행 시 worst_roi=0 리셋 → tier별 독립 디펜스 추적 (2) 배포 시점 기존 LIMIT 선주문은 다음 틱 EP 검증 블록에서 자연 재배치 (3) BC/CB/HEDGE 계열 role은 기존대로 제외. **기대 효과** [실측+추정]: (1) TREND T3 4h+ FC 13건 -$373 중 $50~$150 방어 [추정] (2) 기회상실 +$52 확정 [실측] — TRIM_T3 3h+ 10건 기회상실 (3) T2/T3 디펜스는 worst 로깅 축적 후 1주+ 재튜닝 (4) 순 +$50~$150 [추정 폭 큼]. **한계 [필수 고지]**: (1) 3-4h FC 2건 중 1건(UNI)은 HARD_SL이 아닌 다른 경로로 -5.72%에서 FC — 단계적 limit 체결 가능성 있음 (2) AVAX는 3.8h HARD_SL -10.97% 터짐 — 4h 컷 발동 전 FC, 방어 불가 (3) 4h 시점 평균 roi 실측 불가 — 내부 50% 가정은 [추정] (4) PRE_MKT 비활성 시 주말/휴장/시장 혼란 중 ET 08:30 단체 정리 부재 — 재활성화 원하면 주석 해제. **검증 가능 시점**: 1주+ 누적 후 (1) TREND_T3 FC 건수 -30~50% [추정] (2) worst_roi_seen 분포 확인해 T2_DEF/T3_DEF_M5 임계 재조정 (3) [T2_DEF_ENTER]/[T3_DEF_M5_ENTER] 빈도로 디펜스 트리거 현실성 검증 |
| 04-21 | **슬롯 race condition 버그 수정 + Portfolio TP J안 (Peak Trail Tiered Drop)** (사용자 실측 보고 + 설계) | **버그**: 사용자 보고 "신규진입 롱 5개, 제한 4개". 실측 12:46:20 스냅샷 CORE 포지션 7건 = 롱 6 (XRP/ETH/SUI/XLM/DOT/SOL) + 숏 1 (OP) → MAX_MR_PER_SIDE=4 위반. 원인: 12:42:13~12:43:21 70초 내 HF_MR 롱 4건 연속 진입. 같은 메인 루프 틱 내 `count_slots(st)` 호출 시 이미 발사된 intent가 st 미반영 상태 → 슬롯 카운트 stale → 동일 사이드에 추가 진입 통과. 메인 루프 2초 주기 내 다중 심볼 평가 시 재현. **PTP 설계**: 사용자 의도 = "1% 정도 플러스 후 급격 악화 구간 전 정리" + "포지션 많은 상태에서 추세 들어오면 안되니까 꾸준히 정리". 실측 시뮬 9일: 단순 1% 청산 -$103 (손실), Peak Trail 1%/0.3% +$77, J안 (tiered drop 0.3/0.4/0.5%p) +$124. 사용자 결정: J안 단독 (K gate 보류 — 단순성 우선). 시간 기준은 텔레그램 일일 리포트(_daily_pnl_report)와 통일 — KST 09:00 (UTC 00:00). 자연 쿨다운 (정리 후 _session_start 갱신). **추가 고려했다 기각**: ① 마진율 30%+ AND 조건 (마진 41% 일상 운영 상태 → 필터 무기능 [실측]) ② 단순 1% 청산 (peak 캡 부작용 -$103 [실측]) ③ Ratchet floor + 0.2%p trail (이득 +$152지만 false positive 위험 매우 높음, 일상 변동성 0.2%p 빈번 [실측 04-21 500분 내부 drop 다수]) ④ 즉시 시장가 (수수료 0.16%/발동 vs maker 0.08% — 단계적이 우세 [실측 3x 레버리지 반영]) | V10.31k: **(a) planners.py 슬롯 race 수정** — line 635-636 `_tick_new_long`/`_tick_new_short` 카운터 초기화. line 667-670 MR 진입 슬롯 체크에 `_tick_new_*` 합산 (`_real_long = _slots_mr_pre.risk_long + _tick_new_long`). line 1113-1116 MR 진입 성공 후 `_tick_new_*` += 1. line 1131-1132 COMP 후보 탐색 `_tr_opp_slots`/`_sig_side_slots`에 `_tick_new_*` 합산. line 905-907 NOSLOT 후보 탐색에도 동일 적용. line 1303-1318 NOSLOT 발사 직전 최종 슬롯 재체크 + `[NOSLOT_FINAL_BLOCK]` 로그. **(b) PTP 구현 (J안)** — `config.py:120~155` PTP 상수 (PTP_PEAK_TRIG_PCT=1.0, PTP_DROP_BY_PEAK tiered (2.0,0.5)/(1.5,0.4)/(1.0,0.3) %p, PTP_AVG_TIER_GATE=0.0 [K gate 비활성], PTP_STEP_INTERVAL_SEC=300, PTP_PREMIUMS_BY_STEP {0:0.0020, 1:0.0015, 2:0.0010}, PTP_SESSION_TZ_OFFSET_SEC=0 [UTC=KST09:00]) + `_ptp_get_drop_thresh()` 함수. `planners.py:2426~` `_ptp_session_date_kst` (UTC 자정=KST 09:00 기준), `_ptp_update_state` (peak 갱신 + 세션 리셋 + 트리거 판정), `plan_portfolio_tp` (단계적 청산, T3_3H 패턴 복제). `generate_all_intents` 최우선 위치에 PTP 호출 — 발동 시 early return으로 다른 intent 차단. **(c) order_router.py:116** `is_ptp_limit` 필드 metadata 보존 (단계 간 limit 취소 식별용). **(d) docs/PTP.md** 신규 — J안 구조도 + 실측 시뮬 표 + 수수료 영향 + 체크리스트. **(e) CLAUDE.md** 필수 규칙 line 22(슬롯 race), 23(PTP J안 KST 09:00), 24(시간 통일) 추가. **시간 통일** [중요]: PTP 세션 경계를 텔레그램 `_daily_pnl_report`(UTC 00:00 = KST 09:00)와 동일하게 맞춤. 시스템 전체 일일 리셋 시각 통일 — 향후 일일 기반 모든 로직(예: 일일 진입 한도, 일일 손실 한도)도 동일 기준 따라야 함. **엣지 케이스 처리**: (1) PTP 발동 중 자정 전환 → trigger_ts 등 모든 _ptp_* 상태 리셋, 진행 중 limit은 자연 타임아웃 (2) step 3 시장가 완료 후 _ptp_session_start = current_balance → 또 +1% 쌓여야 재발동 (3) BC/CB/HEDGE 계열 제외 (4) 신규 진입 차단 안 함 — 재진입은 자연스러움 (tier 리셋 목적 부합). **기대 효과** [실측+추정]: (1) 슬롯 버그 12:46 케이스 재현 차단 [실측 PASS] (2) PTP 9일 시뮬 +$124 [실측 — n=9 한계] (3) 발동 빈도 주 3-4회 추정 → 수수료 maker mix $1.73/회 = 주 $7 (4) 순 +$117/주 [추정 폭 ±50%]. **한계 [필수 고지]**: (1) PTP 시뮬은 final 시점만 봄 — 일중 false positive 미반영 (실제 운영 시 발동 빈도 더 높을 수 있음) (2) 04-19 -$98 drop은 peak +$9로 미arming → 가장 큰 drop 못 막음 (Peak Trail 구조적 한계) (3) MR unrealized 평균 -1.3% 상태에서 +0.20% 유리방향 limit 체결 확률 — 30~70% 범위 [추정] (4) K gate 비활성으로 안정 구간(T1만)에서도 발동 가능 → 1주 운영 후 K gate 재도입 검토 (5) 슬롯 race 수정은 같은 틱 race만 해결 — 다른 메커니즘 race 있으면 추가 발견 필요. **검증 가능 시점**: 1주+ 누적 후 (1) [PTP_TRIGGER]/[PTP_S0~S3]/[PTP_COMPLETE] 로그 빈도 (2) 발동당 평균 방어 이득 vs 수수료 비율 (3) [NOSLOT_FINAL_BLOCK] 발생 빈도 — 추가 race 패턴 존재 여부 (4) 자정(UTC 00:00 = KST 09:00) [PTP_SESSION_RESET] 정상 발생 |
| 04-21 | **PTP 재시작 시 세션 복원 로직** (사용자 실측 보고) | 사용자 보고 (V10.31k 배포 직후): "지금 트레일 들어간건가 1프로 넘었던데". 실측 확인 — balance 추이 start $3337.42 → peak $3377.94 (14:17 UTC) = +1.21% 상승했지만 PTP 미발동. 원인 분석: V10.31k 배포(UTC 13:47:04)로 봇 재시작 → `_ptp_update_state` 세션 리셋 블록이 `_ptp_session_date` 미설정 상태 감지 → 현재 balance $3365.11을 세션 시작으로 설정. 그 결과 peak gain이 $3377.94 / $3365.11 = **0.38%만** 계산됨 → PTP_PEAK_TRIG_PCT(1.0%) 미달 → arming 자체 안 됨. 즉 **재시작 시 오늘의 진짜 KST 09:00 start 정보 소실**. 진짜 start는 balance.csv의 첫 레코드 ($3337.42 @ 10:51 UTC). 서버 타임존 확인 — 사용자 "배포 밤 10시" → KST 22:47 = UTC 13:47 → **서버 UTC**, balance.csv 시각도 UTC. 사용자 결정: 복원 로직 필요 | V10.31l: **(a) planners.py 신규 `_load_today_balance_stats(utc_day_start_ts)` 함수** — log_balance.csv 파싱, 오늘 UTC 00:00 이후 첫 레코드 = session_start, 이후 max = session_peak. `time.gmtime + strftime`으로 boundary_str "YYYY-MM-DD HH:MM" 생성 후 CSV 시각과 문자열 비교 (포맷 동일). 파일 미존재 / 파싱 실패 / 오늘 레코드 없음 시 (0.0, 0.0) 반환 → 폴백 경로. **(b) `_ptp_update_state` 세션 리셋 블록 개정** — 복원 시도 후 `restored_start > 0`이면 세션 복원 (`PTP_SESSION_RESTORE` 로그), 실패 시 기존 동작 유지 (`PTP_SESSION_RESET` 로그). 즉 **두 로그 태그로 케이스 구분 가능**. 복원 시 peak는 `max(restored_peak, current_balance)` — 복원된 peak와 현재값 중 큰 것 유지해 정보 손실 없음. **(c) docs/PTP.md** 함정 섹션에 V10.31l 복원 로직 추가. **(d) CLAUDE.md** 필수 규칙 V10.31l 추가 (서버 UTC 가정 명시). **실측 시뮬 PASS**: 04-21 balance.csv 기반 복원 결과 start=$3337.42, peak=$3377.94, peak_gain=1.21% → V10.31l 배포 시 즉시 arming 상태 전환. 현재 drop 0.09%p라 trigger는 아직 대기. **엣지 케이스 처리**: (1) balance.csv 파일 없음 → 폴백 (2) 오늘 UTC 00:00 이후 레코드 없음 → 폴백 (3) 파일 읽기 에러 → 폴백 (4) 레코드 파싱 실패(잘못된 float) → skip. **가정** [필수 고지]: 서버 타임존 UTC. balance.csv 시각 UTC. 사용자 "밤 10시 배포 = UTC 13:47 로그"로 확정. 서버 타임존이 다르면 복원 기준 날짜 틀어져 오작동 가능 — `date` 명령어로 확인 권장. **한계**: (1) balance.csv 로테이션 시 오늘 레코드 일부 소실 가능 → 복원된 start가 진짜 09:00 값이 아닐 수 있음 (2) 봇이 오늘 KST 09:00 전후로 꺼져있었다면 "오늘 첫 레코드"가 실제 09:00 후 몇 시간 뒤 값 → peak 과추정 가능 (3) 재시작 직후 peak > current_balance면 즉시 drop 감지 → 첫 틱에 트리거 가능성. **검증 가능 시점**: 즉시 (배포 후 첫 [PTP_SESSION_RESTORE] 또는 [PTP_SESSION_RESET] 로그 확인), 다음 UTC 00:00 경계 (자동 리셋 시점 정상 동작) |
